{{- if .Values.monitoring.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  dashboard-energy-drain.json: |-
{{ .Files.Get "dashboards/dashboard-lingua-app.json" | indent 4 }}
{{- end }}
